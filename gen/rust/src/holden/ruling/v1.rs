// Code generated by protoc-gen-rust-serde. DO NOT EDIT.
// source: holden/ruling/v1/ruling.proto

use serde::{Deserialize, Serialize};

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Serialize, Deserialize)]
pub enum RulingState {
    #[default]
    #[serde(rename = "RULING_STATE_UNSPECIFIED")]
    RulingStateUnspecified,
    #[serde(rename = "RECEIVED")]
    Received,
    #[serde(rename = "INPUTS_ASSEMBLED")]
    InputsAssembled,
    #[serde(rename = "JUDGE_SPAWNED")]
    JudgeSpawned,
    #[serde(rename = "VERDICT")]
    Verdict,
    #[serde(rename = "REFUSED_RETRY")]
    RefusedRetry,
    #[serde(rename = "PUBLISHED")]
    Published,
    #[serde(rename = "FAILED")]
    Failed,
    #[serde(other)]
    Unknown,
}

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Serialize, Deserialize)]
pub enum Verdict {
    #[default]
    #[serde(rename = "VERDICT_UNSPECIFIED")]
    VerdictUnspecified,
    #[serde(rename = "RATIFY")]
    Ratify,
    #[serde(rename = "BOUNCE")]
    Bounce,
    #[serde(rename = "NEEDS_CLARIFICATION")]
    NeedsClarification,
    #[serde(other)]
    Unknown,
}

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Serialize, Deserialize)]
pub enum ShapeVerdict {
    #[default]
    #[serde(rename = "SHAPE_VERDICT_UNSPECIFIED")]
    ShapeVerdictUnspecified,
    #[serde(rename = "ON_MESH")]
    OnMesh,
    #[serde(rename = "WRONG_SHAPE")]
    WrongShape,
    #[serde(other)]
    Unknown,
}

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Serialize, Deserialize)]
pub enum DocContentAgreement {
    #[default]
    #[serde(rename = "DOC_CONTENT_AGREEMENT_UNSPECIFIED")]
    DocContentAgreementUnspecified,
    #[serde(rename = "AGREE")]
    Agree,
    #[serde(rename = "DISAGREE")]
    Disagree,
    #[serde(rename = "UNCLEAR")]
    Unclear,
    #[serde(other)]
    Unknown,
}

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Serialize, Deserialize)]
pub enum Classification {
    #[default]
    #[serde(rename = "CLASSIFICATION_UNSPECIFIED")]
    ClassificationUnspecified,
    #[serde(rename = "ADDITIVE")]
    Additive,
    #[serde(rename = "BREAKING")]
    Breaking,
    #[serde(rename = "SILENT_DRIFT")]
    SilentDrift,
    #[serde(other)]
    Unknown,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct RulingRequest {
    #[serde(rename = "requestId")]
    pub request_id: String,
    #[serde(rename = "repo")]
    pub repo: String,
    #[serde(rename = "prNumber")]
    pub pr_number: u32,
    #[serde(rename = "includes", skip_serializing_if = "Vec::is_empty")]
    pub includes: Vec<String>,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct RulingHandle {
    #[serde(rename = "rulingId")]
    pub ruling_id: String,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct RulingStatus {
    #[serde(rename = "rulingId")]
    pub ruling_id: String,
    #[serde(rename = "state")]
    pub state: RulingState,
    #[serde(rename = "ruling", skip_serializing_if = "Option::is_none")]
    pub ruling: Option<Ruling>,
    #[serde(rename = "failureReason")]
    pub failure_reason: String,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct RulingEvent {
    #[serde(rename = "rulingId")]
    pub ruling_id: String,
    #[serde(rename = "at", skip_serializing_if = "Option::is_none")]
    pub at: Option<String>,
    #[serde(rename = "state")]
    pub state: RulingState,
    #[serde(rename = "detail")]
    pub detail: String,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct Ruling {
    #[serde(rename = "diffRef")]
    pub diff_ref: String,
    #[serde(rename = "judgeInstance")]
    pub judge_instance: String,
    #[serde(rename = "firedAt", skip_serializing_if = "Option::is_none")]
    pub fired_at: Option<String>,
    #[serde(rename = "verdict")]
    pub verdict: Verdict,
    #[serde(rename = "divergences", skip_serializing_if = "Vec::is_empty")]
    pub divergences: Vec<Divergence>,
    #[serde(rename = "shapeVerdict")]
    pub shape_verdict: ShapeVerdict,
    #[serde(rename = "shapeJustification")]
    pub shape_justification: String,
    #[serde(rename = "consumerImpact", skip_serializing_if = "Vec::is_empty")]
    pub consumer_impact: Vec<ConsumerImpact>,
    #[serde(rename = "docContentAgreement")]
    pub doc_content_agreement: DocContentAgreement,
    #[serde(rename = "ledgerEntryId")]
    pub ledger_entry_id: String,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct Divergence {
    #[serde(rename = "claim")]
    pub claim: String,
    #[serde(rename = "necessary")]
    pub necessary: bool,
    #[serde(rename = "justification")]
    pub justification: String,
}

#[derive(Clone, Debug, Default, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct ConsumerImpact {
    #[serde(rename = "consumer")]
    pub consumer: String,
    #[serde(rename = "classification")]
    pub classification: Classification,
    #[serde(rename = "evidence")]
    pub evidence: String,
}

#[cfg(test)]
mod protojson_tests {
    use super::*;

    #[test]
    fn ruling_state_vocab_and_tolerance() {
        // serializes as the protojson-standard value name (not the bare vocabulary).
        assert_eq!(
            serde_json::to_string(&RulingState::RulingStateUnspecified).unwrap(),
            "\"RULING_STATE_UNSPECIFIED\""
        );
        // an unrecognized protojson value decodes to Unknown, never failing the parse.
        assert_eq!(
            serde_json::from_str::<RulingState>("\"__big_little_mesh_unknown__\"").unwrap(),
            RulingState::Unknown
        );
    }

    #[test]
    fn verdict_vocab_and_tolerance() {
        // serializes as the protojson-standard value name (not the bare vocabulary).
        assert_eq!(
            serde_json::to_string(&Verdict::VerdictUnspecified).unwrap(),
            "\"VERDICT_UNSPECIFIED\""
        );
        // an unrecognized protojson value decodes to Unknown, never failing the parse.
        assert_eq!(
            serde_json::from_str::<Verdict>("\"__big_little_mesh_unknown__\"").unwrap(),
            Verdict::Unknown
        );
    }

    #[test]
    fn shape_verdict_vocab_and_tolerance() {
        // serializes as the protojson-standard value name (not the bare vocabulary).
        assert_eq!(
            serde_json::to_string(&ShapeVerdict::ShapeVerdictUnspecified).unwrap(),
            "\"SHAPE_VERDICT_UNSPECIFIED\""
        );
        // an unrecognized protojson value decodes to Unknown, never failing the parse.
        assert_eq!(
            serde_json::from_str::<ShapeVerdict>("\"__big_little_mesh_unknown__\"").unwrap(),
            ShapeVerdict::Unknown
        );
    }

    #[test]
    fn doc_content_agreement_vocab_and_tolerance() {
        // serializes as the protojson-standard value name (not the bare vocabulary).
        assert_eq!(
            serde_json::to_string(&DocContentAgreement::DocContentAgreementUnspecified).unwrap(),
            "\"DOC_CONTENT_AGREEMENT_UNSPECIFIED\""
        );
        // an unrecognized protojson value decodes to Unknown, never failing the parse.
        assert_eq!(
            serde_json::from_str::<DocContentAgreement>("\"__big_little_mesh_unknown__\"").unwrap(),
            DocContentAgreement::Unknown
        );
    }

    #[test]
    fn classification_vocab_and_tolerance() {
        // serializes as the protojson-standard value name (not the bare vocabulary).
        assert_eq!(
            serde_json::to_string(&Classification::ClassificationUnspecified).unwrap(),
            "\"CLASSIFICATION_UNSPECIFIED\""
        );
        // an unrecognized protojson value decodes to Unknown, never failing the parse.
        assert_eq!(
            serde_json::from_str::<Classification>("\"__big_little_mesh_unknown__\"").unwrap(),
            Classification::Unknown
        );
    }

    #[test]
    fn ruling_request_protojson() {
        let d = RulingRequest::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<RulingRequest>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("requestId").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("repo").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("prNumber").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("includes").is_some());
    }

    #[test]
    fn ruling_handle_protojson() {
        let d = RulingHandle::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<RulingHandle>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("rulingId").is_some());
    }

    #[test]
    fn ruling_status_protojson() {
        let d = RulingStatus::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<RulingStatus>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("rulingId").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("state").is_some());
        // an unset Option (message / timestamp / proto3-optional) is omitted on encode.
        assert!(jd.get("ruling").is_none());
        // carries its lowerCamel protojson key.
        assert!(jd.get("failureReason").is_some());
        // a present Option serializes under its protojson key (Timestamp rides as a string).
        let mut p = d.clone();
        p.ruling = Some(Default::default());
        let jp = serde_json::to_value(&p).unwrap();
        assert!(jp.get("ruling").is_some());
    }

    #[test]
    fn ruling_event_protojson() {
        let d = RulingEvent::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<RulingEvent>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("rulingId").is_some());
        // an unset Option (message / timestamp / proto3-optional) is omitted on encode.
        assert!(jd.get("at").is_none());
        // carries its lowerCamel protojson key.
        assert!(jd.get("state").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("detail").is_some());
        // a present Option serializes under its protojson key (Timestamp rides as a string).
        let mut p = d.clone();
        p.at = Some(Default::default());
        let jp = serde_json::to_value(&p).unwrap();
        assert!(jp.get("at").is_some());
    }

    #[test]
    fn ruling_protojson() {
        let d = Ruling::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<Ruling>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("diffRef").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("judgeInstance").is_some());
        // an unset Option (message / timestamp / proto3-optional) is omitted on encode.
        assert!(jd.get("firedAt").is_none());
        // carries its lowerCamel protojson key.
        assert!(jd.get("verdict").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("divergences").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("shapeVerdict").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("shapeJustification").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("consumerImpact").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("docContentAgreement").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("ledgerEntryId").is_some());
        // a present Option serializes under its protojson key (Timestamp rides as a string).
        let mut p = d.clone();
        p.fired_at = Some(Default::default());
        let jp = serde_json::to_value(&p).unwrap();
        assert!(jp.get("firedAt").is_some());
    }

    #[test]
    fn divergence_protojson() {
        let d = Divergence::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<Divergence>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("claim").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("necessary").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("justification").is_some());
    }

    #[test]
    fn consumer_impact_protojson() {
        let d = ConsumerImpact::default();
        // proto3 omits defaults: every field absent on the wire decodes to the default.
        assert_eq!(serde_json::from_str::<ConsumerImpact>("{}").unwrap(), d);
        let jd = serde_json::to_value(&d).unwrap();
        // carries its lowerCamel protojson key.
        assert!(jd.get("consumer").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("classification").is_some());
        // carries its lowerCamel protojson key.
        assert!(jd.get("evidence").is_some());
    }

}
